<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.project_protal">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:appComponentFactory="androidx"
        android:icon="@drawable/ic_bookshelf"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_bookshelf"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:replace="android:appComponentFactory">
        <activity android:name=".MapsActivity2"></activity>
        <activity android:name=".Pay_monthly_fee"></activity>
        <activity android:name=".Manage_Fees" />
        <activity android:name=".Massage_view" />
        <activity android:name=".timeTest" />
        <activity android:name=".No_connection" />
        <activity android:name=".Change_password" />
        <activity android:name=".Forget_password" />
        <activity android:name=".volly" />
        <activity android:name=".student_chart"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyB7IZIaUyX8PgtGGw1GBVBA6HB3oMEvLMQ"

            />
<!--            android:value="AIzaSyBFJEKBg8NgWNEF5CB76XpxIH_xgNTBxc4"-->



        <activity
            android:name=".MapsActivity"
            android:label="@string/location" />
        <activity android:name=".view_location"></activity>
        <activity android:name=".emailVerifivation" />
        <activity android:name=".View_profile" />
        <activity
            android:name=".User_navigation"
            android:label="@string/title_activity_user_navigation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Dash_board_navigation"
            android:label="@string/title_activity_dash_board_navigation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Massage_send" />
        <activity android:name=".Time_table_view" />
        <activity android:name=".Add_timetable" />
        <activity android:name=".Select_batch" />
        <activity android:name=".Dash_bord" />
        <activity android:name=".Start_anim">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View" />
        <activity android:name=".Send" />
        <activity android:name=".Chating" />
        <activity android:name=".User" />
        <activity android:name=".Register_batch" />
        <activity android:name=".View_students" />
        <activity android:name=".Sign_up" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>